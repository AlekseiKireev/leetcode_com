## инвариант (строгая формулировка)

Пусть массив отсортирован:
`n[0] ≤ n[1] ≤ ... ≤ n[k]`

### 🔒 Инвариант

> В начале каждой итерации цикла **не существует решения** с индексами:
>
> * `i' < i`
> * `j' > j`

То есть:

* всё **левее `i`** уже исключено
* всё **правее `j`** уже исключено

Мы всегда ищем решение **только внутри интервала** `[i, j]`.

---

## Корректная версия разбора трёх случаев

### 📌 Условие

Рассматриваем `i < j`, массив отсортирован.

---

### 🔹 Случай 1:

`n[i] + n[j] < target`

#### Что это означает?

* При **фиксированном `i`**
* Для любого `k ≤ j`:

```
n[i] + n[k] ≤ n[i] + n[j] < target
```

➡️ **ни один индекс `k ≤ j` не даст target**

#### Почему нельзя уменьшать `j`?

Уменьшение `j` только **уменьшит сумму ещё сильнее**.

#### Следствие

* Текущий `i` **не может участвовать в решении**
* Значит, можно безопасно исключить `i`

✅ **Действие:** `i++`

---

### 🔹 Случай 2:

`n[i] + n[j] > target`

#### Что это означает?

* При **фиксированном `j`**
* Для любого `k ≥ i`:

```
n[k] + n[j] ≥ n[i] + n[j] > target
```

➡️ **ни один индекс `k ≥ i` не даст target**

#### Почему нельзя увеличивать `i`?

Увеличение `i` только **увеличит сумму ещё сильнее**.

#### Следствие

* Текущий `j` **не может участвовать в решении**
* Значит, можно безопасно исключить `j`

✅ **Действие:** `j--`

---

### 🔹 Случай 3:

`n[i] + n[j] == target`

🎯 Найдено решение.

---


## Короткая идеальная формулировка для собеса

> Инвариант алгоритма двух указателей:
> На каждой итерации все пары с индексами меньше `i` или больше `j` уже логически исключены.
> Если сумма меньше target — левый индекс не может участвовать в решении, если больше — правый.

---


